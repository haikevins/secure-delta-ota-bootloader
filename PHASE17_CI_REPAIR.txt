Phase 17 CI repair patch

Extract this ZIP directly into the repository root so the hidden .github
directory is updated.

Expected files:
  .github/workflows/build.yml
  .github/workflows/test.yml
  .github/workflows/firmware-release.yml

After extraction:
  python3 scripts/phase17_check.py --static-only
  make phase17-check TOOLCHAIN=gcc
